server:
    port: 4042

spring:
    datasource:
        url: jdbc:h2:mem:tbotdb;MODE=MySQL;DATABASE_TO_UPPER=FALSE;CASE_INSENSITIVE_IDENTIFIERS=TRUE;INIT=RUNSCRIPT FROM 'classpath:/db/init-it-db.sql'
        username: sa
        password: ''
        hikari:
            schema: ruler
    liquibase:
        liquibase-schema: ruler
        default-schema: ruler
    h2:
        console:
            enabled: true
